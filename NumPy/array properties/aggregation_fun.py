import numpy as np

arr = np.array([10, 20, 30, 40, 50])

# sum of array
print(np.sum(arr))

# average of array
print(np.mean(arr))

# min of array
print(np.min(arr))

# max of array
print(np.max(arr))

# standard deviation of array
print(np.std(arr))

# variance of array
print(np.var(arr))